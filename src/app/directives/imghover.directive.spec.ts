import { ImghoverDirective } from './imghover.directive';

describe('ImghoverDirective', () => {
  it('should create an instance', () => {
    const directive = new ImghoverDirective();
    expect(directive).toBeTruthy();
  });
});
